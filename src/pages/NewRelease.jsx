import React from "react";
import Container from "../components/Container";
import SectionTitle from "../components/SectionTitle";

const NewRelease = () => {
  return (
    <Container>
      <SectionTitle title={"New Realese page"} />
    </Container>
  );
};

export default NewRelease;
